#!/bin/sh

echo "🔎 TypeScript typecheck..."
npm run typecheck || exit 1

echo "🧹 ESLint (no warnings allowed)..."
npm run lint || exit 1

echo "💅 Prettier format check..."
npm run format:check || exit 1
